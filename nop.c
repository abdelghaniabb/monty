#include "monty.h"

/**
 * nop - dosen't do anything.
 * @stack: the head of the doubly linked list represented the stack
 * @line_number: the line number in the file
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number __attribute__((unused)))
{
	(void)stack;
}
